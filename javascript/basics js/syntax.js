// single line cooment
/*multiple 
line 
comment*/

var a=10;//write ; if u prefer
var b=20//omit; if u prefer

let c=30;
const d=40;

c++;
// d++; since d is const we cannot increment the value of d
// error:
//TypeError: Assignment to constant variable. will be generated
console.log(c);